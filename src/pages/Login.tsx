import Button from "@/components/Button";

const Login = () => {
    const handleClick = () => {};

    return (
        <div>
            Login page
            <Button onClick={handleClick} />
        </div>
    );
};

export default Login;
